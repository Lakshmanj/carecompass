# Welfare Compass

Install node modules (npm install) in both client (cd client) and server (cd server) directories. Change directory into server and run it (npm run dev) then open a new terminal and do the same for client.